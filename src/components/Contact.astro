---
import { Mail, MessageCircle, Send } from 'lucide-react';
---

<section id="contact" class="py-20 bg-gray-900/50" data-animate>
  <div class="container mx-auto px-6">
    <h2 class="text-4xl font-bold text-center mb-16">
      <span class="text-gradient">
        Hablemos de tu proyecto
      </span>
    </h2>

    <div class="max-w-4xl mx-auto grid md:grid-cols-2 gap-12">
      <!-- Contact Info -->
      <div class="space-y-8">
        <div>
          <h3 class="text-2xl font-semibold mb-6">¿Tienes una idea? ¡Hagámosla realidad!</h3>
          <p class="text-gray-400 text-lg">
            Especializado en automatización, e-commerce y desarrollo web moderno. 
            Desde MVPs hasta plataformas empresariales completas.
          </p>
        </div>

        <div class="space-y-6">
          <div class="flex items-center space-x-4">
            <div class="w-12 h-12 bg-blue-500/20 rounded-lg flex items-center justify-center">
              <Mail class="w-6 h-6 text-blue-400" />
            </div>
            <div>
              <div class="font-semibold">Email</div>
              <a href="mailto:ferbasiliorengifo@gmail.com" class="text-blue-400 hover:text-blue-300 transition-colors">
                ferbasiliorengifo@gmail.com
              </a>
            </div>
          </div>

          <div class="flex items-center space-x-4">
            <div class="w-12 h-12 bg-green-500/20 rounded-lg flex items-center justify-center">
              <MessageCircle class="w-6 h-6 text-green-400" />
            </div>
            <div>
              <div class="font-semibold">WhatsApp</div>
              <a href="https://wa.me/51933484150" class="text-green-400 hover:text-green-300 transition-colors">
                +51 933 484 150
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- Contact Form -->
      <div class="glass-effect rounded-xl p-8">
        <form class="space-y-6" action="mailto:hola@atter.dev" method="post" enctype="text/plain">
          <div>
            <label for="name" class="block text-sm font-medium mb-2">Nombre</label>
            <input 
              type="text" 
              id="name" 
              name="name"
              required
              class="w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
              placeholder="Tu nombre completo"
            />
          </div>

          <div>
            <label for="email" class="block text-sm font-medium mb-2">Email</label>
            <input 
              type="email" 
              id="email" 
              name="email"
              required
              class="w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
              placeholder="tu@email.com"
            />
          </div>

          <div>
            <label for="project" class="block text-sm font-medium mb-2">Tipo de proyecto</label>
            <select 
              id="project" 
              name="project"
              class="w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
            >
              <option value="">Selecciona una opción</option>
              <option value="webapp">Aplicación Web</option>
              <option value="ecommerce">Tienda Online</option>
              <option value="automation">Automatización</option>
              <option value="chatbot">Chatbot IA</option>
              <option value="other">Otro</option>
            </select>
          </div>

          <div>
            <label for="message" class="block text-sm font-medium mb-2">Mensaje</label>
            <textarea 
              id="message" 
              name="message"
              rows="4"
              required
              class="w-full px-4 py-3 bg-gray-800/50 border border-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all resize-none"
              placeholder="Cuéntame sobre tu proyecto..."
            ></textarea>
          </div>

          <button 
            type="submit"
            class="w-full bg-gradient-to-r from-blue-500 to-purple-600 text-white px-6 py-3 rounded-lg font-semibold hover:scale-105 transition-transform flex items-center justify-center space-x-2"
          >
            <Send class="w-5 h-5" />
            <span>Enviar mensaje</span>
          </button>
        </form>
      </div>
    </div>
  </div>
</section>